apiVersion: v1
kind: Service
metadata:
  name: {{ include "inference-benchmark-backend.fullname" . }}
  labels:
    {{- include "inference-benchmark-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inference-benchmark-backend.selectorLabels" . | nindent 4 }}
