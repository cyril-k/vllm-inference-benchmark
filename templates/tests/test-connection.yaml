apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inference-benchmark-backend.fullname" . }}-test-connection"
  labels:
    {{- include "inference-benchmark-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inference-benchmark-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
